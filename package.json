{
  "name": "simple-server-nodejs",
  "type": "module",
  "version": "1.0.0",
  "description": "simple server that responds to ad requests",
  "main": "./src/http-server.js",
  "scripts": {
    "test": "mocha --opts ./mocha.opts --require babel-core/register ${MOCHA_ARGS}",
    "test-watch": "npm run test -- -w --watch-extensions js",
    "build": "babel -d dist/ src",
    "start": "npm run build && node ./dist/main.js"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "babel": "^6.23.0",
    "babel-core": "^6.26.3",
    "babel-preset-es2015": "^6.24.1",
    "chai": "^4.2.0",
    "mocha": "^6.2.2",
    "sinon": "^7.5.0",
    "sinon-chai": "^3.3.0",
    "supertest": "^4.0.2"
  },
  "dependencies": {
    "babel-cli": "^6.26.0"
  }
}
